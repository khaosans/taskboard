import React from 'react';

const Header: React.FC = () => {
    return (
        <header>
            <h1>Your App Name</h1>
            {/* Add more header content as needed */}
        </header>
    );
};

export default Header;
