import React from 'react';
import SwapWidget from '@/components/swap-widget';

const SwapPage = () => {
    return (
        <div>
            <h1>Token Swap</h1>
            <SwapWidget />
        </div>
    );
};

export default SwapPage;
